<!DOCTYPE html>
<html>
    <head>
        <title>Resources</title>
        <link rel="icon" href="/img/weather.png">
        <link rel="stylesheet" href="/css/styles.css">
    </head>
    <body>
        <div class="main-content">
            {{>header}}
            <p>{{content}}</p>
            <ul>
                <li><a target="_blank" href={{weatherAPI.link}}>{{weatherAPI.name}}</a></li>
                <li><a target="_blank" href={{mapbox.link}}>{{mapbox.name}}</a></li>
                <li><a target="_blank" href={{request.link}}>{{request.name}}</a></li>
                <li><a target="_blank" href={{handlebars.link}}>{{handlebars.name}}</a></li>
                <li><a target="_blank" href={{express.link}}>{{express.name}}</a></li>
            </ul>
        </div>
        
        {{>footer}}
    </body>
</html>